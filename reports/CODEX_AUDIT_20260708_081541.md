VERDICT: FAIL

## Findings

1. `aleph/explore/vault.py:32` — `grail.md` 禁止が Vault 直下にしか効きません。  
   `rel.name == "grail.md" and rel.parent == Path(".")` のため、`wiki/grail.md` や `concepts/grail.md` は読み取り可能になります。PLAN は `grail.md` を「読まない・触らない」とし、`config/policies.yaml:37` も `forbidden: [grail.md]` と名前ベースで宣言しています。Vault 内に同名ファイルが増えた場合、人間所有の非公開メモを ALEPH が素材カード化できるセキュリティ/プライバシーリスクです。

2. `tests/test_m0_acceptance.py:65` — `calls.jsonl` の受入テストが再現性要件を十分に検査していません。  
   `aleph/core/llm.py:90` では `params` と `usage` も必須フィールドですが、テストは `ts/role/provider/model/prompt_hash/response_hash/cost_usd` だけを確認しています。M0実装が temperature、max_tokens、logprobs、seed、token usage を記録しなくても受入テストを通過でき、PLAN §1.1/§2.1 の「全LLM呼び出しを再現可能に記録する」要件を満たさない実装を許します。

3. `tests/test_m0_acceptance.py:78` — Router が予算超過時にプロバイダ呼び出しを止めることを検査していません。  
   現在のテストは `Budget` 単体の `charge/precheck` だけです。`Router.call()` が `budget.precheck()` を呼ばずに外部APIを叩く実装でも、このテストは通ります。PLAN と `aleph/core/llm.py:110` は「予算の事前照会、超過なら呼ばずに BudgetExceeded」を要求しているため、従量APIで上限超過の実費が発生する失敗シナリオがあります。

## Verification

`uv run pytest` と `uv run pytest -m m0` は read-only サンドボックスで `~/.cache/uv` を初期化できず未実行です。静的レビューで上記を確認しました。