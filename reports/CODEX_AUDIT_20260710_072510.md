VERDICT: FAIL

## Findings

1. **Goodhart 防壁が構造的に破れている**  
   `aleph/critique/review.py:54`, `aleph/critique/review.py:103`, `aleph/draft/revise.py:33`, `aleph/compose/generate.py:190`  
   Failure scenario: jury/critic が `{"score": 7.5, "critique": "score 7.5。附則が弱い"}` のように、批評文中へ点数を再掲すると、その文字列がそのまま author の改稿/進化プロンプトへ入ります。M3/M4 契約の「author プロンプトへの数値スコア混入禁止」に反します。現行テストは sanitized な `critiques` を直接 `revise()` に渡すため、この閉ループ経由の漏れを見逃しています。

2. **`aleph run` の新経路で予算が永続化されず、プロセスをまたぐ上限が効かない**  
   `aleph/cli.py:48`, `aleph/core/budget.py:67`  
   Failure scenario: `uv run aleph run --work w0001` が API 予算を消費して終了しても、次回起動時に `Budget(config)` が空のインメモリ台帳を作るため、月次 `$10` と作品別 `$3` の実消費が復元されません。クラッシュ再開や複数作品実行で上限を容易に超えます。`Budget` 側のコメントにも CLI は `state_path` を渡すべきとあります。

3. **pricing 宣言は実計上だけに使われ、事前予算照会に使われていない**  
   `aleph/core/llm.py:220`, `aleph/core/llm.py:247`, `aleph/core/budget.py:107`  
   Failure scenario: `author_primary` は output `$50/MTok` ですが、precheck は provider 概算 `$30/MTok` で見積もります。残予算が少ない状態で precheck は通り、応答後に pricing で高い実費が `charge()` され、上限超過後に止まります。「超過が予見される消費は実行前に拒否」の不変条件を満たしていません。

4. **RealDeps の擱筆判断が必要データを渡しておらず、通常の停止経路が実質動かない**  
   `aleph/pipeline.py:165`, `aleph/pipeline.py:335`, `aleph/pipeline.py:346`, `aleph/critique/review.py:223`  
   Failure scenario: `trajectory.jsonl` には `novelty_dist` が保存されず、`instructions_history=[]` 固定なので、収束経路も過剰彫琢経路も成立しません。`run_work()` は stop=false のまま最大 5 回 `critique_and_revise()` を繰り返し、最後は「改稿上限」で FINISH します。M5 の停止契約と予算切れ経路の配線として不十分です。

5. **CLI 経由の Work が secrets を持たず、seed/decisions の scrub が効かない**  
   `aleph/cli.py:42`, `aleph/cli.py:109`, `aleph/core/artifacts.py:100`  
   Failure scenario: `aleph new --hint <秘密値>` や LLM 由来の decision reason に `.env` の値が混入した場合、`Work(..., secrets=())` のため `seed.json` / `decisions.jsonl` へ平文で残ります。`CallLogger` は secrets を受け取っていますが、作品成果物側の書き出し経路は同じ保護を受けていません。

6. **L3 で集めた素材カードが構成・執筆に渡らない**  
   `aleph/pipeline.py:311`, `aleph/draft/write.py:98`  
   Failure scenario: `RealDeps.gather_materials()` が hidden similarity の素材カードを保存しても、`compose_and_draft()` は `materials` を捨て、`pipeline_to_draft()` は `generate_proposals(..., [], ...)` を呼ぶため、author の構成プロンプトは常に「素材なし」になります。PLAN §6.1 の「intent + niche + 選抜素材カード群」を入力に構成する契約を満たしません。

## Verification

- `uv run pytest -p no:cacheprovider`  
  PASS: 17 passed, 79 deselected.

- `uv run pytest -p no:cacheprovider -m "m2 or m3 or m4 or m5 or m6"`  
  PASS: 44 passed, 52 deselected.

- `uv run pytest -p no:cacheprovider -m "not local"`  
  PASS: 95 passed, 1 deselected.

`UV_CACHE_DIR` / `XDG_CACHE_HOME` は `.codex-audit-cache` に向け、実行後に削除済みです。Source files were not modified.