VERDICT: FAIL

## Findings

1. `aleph/core/llm.py:169` / `aleph/core/llm.py:177` — local 台帳の予算超過が Router 経由で防止されません。  
   `Router._charge_amount()` と `_precheck_amount()` が local を常に `0.0` と扱うため、`config/budgets.yaml` の `local.gpu_hours_per_day: 8` に到達済みでも `router.call("scout", ...)` が実行されます。追加再現では `budget.charge("local", 8)` 後でも provider が1回呼ばれ、`local.spent` は `8.0` のままでした。PLAN §2.1/§14.1 の「local=GPU時間を別台帳で計上」「予算超過が予見される呼び出しは実行前に拒否」に反します。  
   テストギャップ: harness の同種バグは `tests/test_m0_regressions.py:42` で覆われていますが、local 台帳の上限到達時に Router が呼び出しを止めるテストがありません。`test_local_swap` はハードウェア依存で skip され、予算計上も検証していません。

2. `aleph/core/llm.py:453` / `aleph/core/llm.py:455` — harness prompt がコマンドライン引数に載るため、未公開本文や秘密混入時にプロセス一覧へ露出します。  
   `subprocess.run(["claude", "-p", prompt], ...)` / `["codex", "exec", prompt]` は shell は使っていませんが、prompt 全文は実行中の argv として見えます。長文ドラフトや素材カードを `author_harness` に渡すと、同一ホスト上の別プロセス/ユーザーが `ps` 等で内容を読める可能性があります。PLAN はログ本文を避けて hash 化しているので、この経路は同じプライバシー方針と噛み合っていません。stdin 経由など argv に本文を置かない実装が必要です。

## Verification

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest`  
  PASS: 17 passed, 17 deselected.

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest -m m0`  
  PASS: 16 passed, 1 skipped, 17 deselected. `test_local_swap` は `ALEPH_LOCAL=1` なしのため skip。

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest -m 'not local'`  
  PASS: 33 passed, 1 deselected.

- `git diff --check 3e1603c...HEAD`  
  FAIL: 追加された `reports/CODEX_AUDIT_*.md` に trailing whitespace。実装挙動の問題ではありません。

- 追加再現: local 予算上限到達後の Router 呼び出し  
  FAIL確認: `NO_EXCEPTION provider_calls=1 status={'spent': 8.0, 'limit': 8, 'period': 'day'}`。

監査用 `.codex-audit-cache` は削除済みです。