VERDICT: FAIL

## Findings

1. `aleph/core/llm.py:208` / `aleph/core/llm.py:231` — Router経由のAPI呼び出しで作品別予算 `usd_per_work` が一切効きません。  
   `Budget.precheck()` / `Budget.charge()` は `work_id` を受け取れるようになっていますが、`Router._invoke()` は `work_id` を渡す手段を持たず、常にグローバルAPI台帳だけを更新します。具体例: `config/budgets.yaml:40` の `usd_per_work: 3.0` に対し、FakeProviderが1回 `$2.0` のAPI応答を返す条件で `router.call("author_primary", ...)` を2回実行しても、`BudgetExceeded` は出ず `api.spent=4.0` まで進みます。PLAN §2.1 の「作品ごと・日ごとの上限」に反します。  
   テストギャップ: `tests/test_m0_regressions.py:203` は `Budget` 単体の永続化だけを検証しており、実運用経路である `Router -> Budget` の作品別上限を覆っていません。

## Verification

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest`  
  PASS: 17 passed, 16 deselected.

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest -m m0`  
  PASS: 15 passed, 1 skipped, 17 deselected. `test_local_swap` は `ALEPH_LOCAL=1` なしのためskip。

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest -m 'not local'`  
  PASS: 32 passed, 1 deselected.

- 追加再現: Router API作品別上限ケース  
  FAIL確認: `usd_per_work=3.0` 相当を超える `$4.0` までRouter経由で進み、`BudgetExceeded` なし。

`.codex-audit-cache` は削除済みです。