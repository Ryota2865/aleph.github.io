VERDICT: FAIL

## Findings

1. `aleph/core/llm.py:182` — Routerの予算事前照会が常に`amount=0.0`なので、上限到達済みでも呼び出しが実行されます。  
   具体例: `harness`を`calls_per_day=40`まで消費済みにして`Router.call("author_harness", ...)`すると、`BudgetExceeded`なしでproviderが1回呼ばれ、台帳は`41/40`になります。PLAN §2.1/M0受入の「予算超過で例外」「超過が予見される呼び出しは実行前に拒否」に反します。`tests/test_m0_acceptance.py:78`は`Budget.precheck("api", 0.01)`を直接見るだけで、Router経由の既知消費量（harness=1 call）を検証していません。

2. `aleph/core/llm.py:409` — harness利用規約適合のM0監査項目が満たせていません。  
   実装は`claude -p`/`codex exec`を無条件に`subprocess.run`で自動実行しますが、PLAN §15-1はM0監査で各社規約の一次判定を記録することを要求しています。Anthropic Consumer Termsは、APIキーまたは明示許可がある場合を除き、自動・非人間手段でのアクセスを禁止しています（[Anthropic Consumer Terms](https://www.anthropic.com/legal/consumer-terms), lines 37-46）。OpenAI Termsも自動/プログラム的なOutput抽出、rate limit回避、競合モデル開発利用を禁止しています（[OpenAI Terms of Use](https://openai.com/policies/terms-of-use/), lines 49-56）。少なくともこの変更には「harnessを有効化する前の明示的な設定/確認/無効化ガード」がなく、PROGRESS.mdの残置監査項目を完了扱いにできません。

3. `aleph/core/loop.py:111` — checkpointから再開した後の遷移で`step`が巻き戻ります。  
   具体例: `checkpoint.json`が`state=DRAFT, step=7`の状態で新しい`Loop`を作り`transition(State.CRITIQUE, ...)`すると、保存後の`checkpoint.json`は`step=1`になります。クラッシュ後再開時に遷移番号が単調増加せず、監査ログとcheckpointの順序が壊れます。M0受入の「チェックポイントから再開できる」は状態ロードだけでなく、再開後の次遷移も保持すべきです。現行テストは`Checkpoint.load()`のみで、`Loop.transition()`再開ケースを覆っていません。

4. `aleph/core/budget.py:126` — 永続化時に作品別API消費`_work_spent`が保存されません。  
   具体例: `state_path`ありでwork Aに`$2.90` chargeしてプロセス再起動すると、月次`api.spent`は復元されますがwork Aのサブ台帳は`0`に戻ります。その後work Aに`$0.20`を`precheck(..., work_id="A")`しても`usd_per_work=3.0`超過を検出できません。PLAN §2.1の「作品ごと・日ごとの上限」に対する永続運用の穴です。テストも永続化と`work_id`上限を検証していません。

## Verification

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest`  
  PASS: 17 passed, 8 deselected.

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest -m m0`  
  PASS: 7 passed, 1 skipped, 17 deselected. `test_local_swap`は`ALEPH_LOCAL=1`なしのためskip。

- `export UV_CACHE_DIR="$PWD/.codex-audit-cache" XDG_CACHE_HOME="$PWD/.codex-audit-cache"; uv run pytest -m 'not local'`  
  PASS: 24 passed, 1 deselected.

- 追加再現: Router harness上限到達ケース  
  FAIL確認: providerが呼ばれ、`harness_spent=41.0`, `harness_limit=40`。

- 追加再現: checkpoint step再開ケース  
  FAIL確認: `step=7`から遷移後に`checkpoint_step_after_resume_transition=1`。

監査用に作成した`.codex-audit-cache`は削除済みです。