schema_version: 1
instruments:
  - {id: novelty.atlas_cosine, version: v1, status: provisional, claim: "Nearest Atlas cosine distance, not literary novelty.", inputs: [draft_bytes, segmentation, embedder, atlas_identity], outputs: [nearest_dist, nearest_chunk_id], calibration_date: uncalibrated, known_counterexamples: ["cell novelty is a different quantity"], blind_spots: ["long-text averages hide local imitation"], next_calibration_condition: "Rebuild a full AtlasIdentity.", unit: cosine_distance, direction: higher, comparability_keys: [instrument_version, identities.atlas_identity, identities.embedder, identities.segmentation], required_identities: [atlas_identity, embedder, segmentation]}
  - {id: form_fidelity.structural_features, version: v1, status: calibrated_limited, claim: "Registered structural-feature retention rate.", inputs: [kind, source_bytes, generated_bytes, detector], outputs: [retention_rate, feature_sets, applicability], calibration_date: '2026-07-17', known_counterexamples: ["content cosine is not structural fidelity"], blind_spots: ["feature presence does not establish aesthetic function"], next_calibration_condition: "Register ten human-judged examples for each new kind.", unit: ratio, direction: higher, comparability_keys: [instrument_version, identities.detector, identities.kind, identities.feature_set], required_identities: [detector, kind, feature_set]}
  - {id: fixation.poetics_lexical, version: v1, status: calibrated_limited, claim: "Adjacent poetics-version character 2-gram self-similarity.", inputs: [poetics_versions, threshold, detector], outputs: [jaccard_series, mean, threshold_exceeded], calibration_date: m5_fixture, known_counterexamples: ["does not detect cross-work fixation"], blind_spots: ["excludes rhetoric and world type"], next_calibration_condition: "Create positive and negative version fixtures.", unit: jaccard, direction: higher, comparability_keys: [instrument_version, identities.detector, identities.threshold, identities.tokenization], required_identities: [detector, threshold, tokenization]}
  - {id: fixation.house_style, version: v1, status: provisional, claim: "Cross-work rhetorical and world-type repetition.", inputs: [work_text, marker_schema, classifier, author_epoch], outputs: [marker_rates, variance, roles, unclassified_rate], calibration_date: uncalibrated, known_counterexamples: ["quotation transformation is not automatic resolution"], blind_spots: ["classifier vocabulary can be Goodharted"], next_calibration_condition: "Check sealed counterexample fixtures.", unit: ratio, direction: higher, comparability_keys: [instrument_version, identities.classifier, identities.prompt, identities.schema, identities.segmentation, identities.author_epoch, identities.collection_manifest], required_identities: [classifier, prompt, schema, segmentation, author_epoch, collection_manifest]}
  - {id: jury.disagreement_stddev, version: v1, status: provisional, claim: "Population standard deviation of valid juror scores.", inputs: [scores, slots, roster, packet], outputs: [stddev, mean, valid_counts, invalid_reasons], calibration_date: uncalibrated, known_counterexamples: ["max-min is a different disagreement measure"], blind_spots: ["invalid exclusions can lower apparent disagreement"], next_calibration_condition: "Re-evaluate convergence and destruction fixtures.", unit: score_stddev, direction: lower, comparability_keys: [instrument_version, identities.score_scale, identities.jury_roster, identities.packet, identities.validity_rules], required_identities: [score_scale, jury_roster, packet, validity_rules]}
  - {id: reader.mean_logprob, version: v1, status: provisional, claim: "Reader mean token logprob, not perplexity.", inputs: [segment_bytes, reader_model, tokenizer, context], outputs: [mean_logprob, optional_perplexity, missing_reason], calibration_date: uncalibrated, known_counterexamples: ["perplexity artifact labels may show mean logprob"], blind_spots: ["surprise is not literary efficacy"], next_calibration_condition: "Test fixed low and high probability fixtures.", unit: logprob, direction: lower, comparability_keys: [instrument_version, identities.model, identities.tokenizer, identities.context, identities.segment, identities.prompt], required_identities: [model, tokenizer, context, segment, prompt]}
  - {id: parse.reliability, version: v1, status: provisional, claim: "Valid projection rate for a preregistered schema.", inputs: [raw_response, schema, prompt, model, slot, retry_policy], outputs: [counts, failure_taxonomy, retries, final_status], calibration_date: uncalibrated, known_counterexamples: ["completed calls can still have parse failure"], blind_spots: ["valid JSON can be semantically invalid"], next_calibration_condition: "Verify slot hardening with retry fixtures.", unit: ratio, direction: higher, comparability_keys: [instrument_version, identities.model, identities.prompt, identities.schema, identities.parser, identities.retry_policy, identities.batch_manifest], required_identities: [model, prompt, schema, parser, retry_policy, batch_manifest]}
  - {id: run.completion, version: v1, status: provisional, claim: "Whether a preregistered run reached its natural endpoint.", inputs: [transition_events, batch_manifest, reservations, termination], outputs: [completion_category, evidence_event], calibration_date: uncalibrated, known_counterexamples: ["resource stop is not normal short completion"], blind_spots: ["terminal state does not prove closing inputs complete"], next_calibration_condition: "Test normal, short, and interrupted fixtures.", unit: category, direction: none, comparability_keys: [instrument_version, identities.run_manifest, identities.closing_batch, identities.stop_rules], required_identities: [run_manifest, closing_batch, stop_rules]}
  - {id: cost.reconciled_usd, version: v1, status: provisional, claim: "API cost reconciled across call, charge, and provider records.", inputs: [call_records, charge_records, provider_records, scope], outputs: [amount, currency, reconciliation_status, unreconciled_breakdown], calibration_date: uncalibrated, known_counterexamples: ["missing provider statement is unreconciled"], blind_spots: ["timeouts and price changes can escape automatic scope"], next_calibration_condition: "Perform a three-way reconciliation on one run.", unit: USD, direction: lower, comparability_keys: [instrument_version, identities.cost_envelope, identities.provider_pricing_date, identities.model, identities.reconciliation_rules], required_identities: [cost_envelope, provider_pricing_date, model, reconciliation_rules]}
